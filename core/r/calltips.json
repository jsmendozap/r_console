{
    "methods": "get_layer insert_layer layer_info list_layers",
    "calltips": {
        "qgis$get_layer(":    "get_layer(x, ...)",
        "qgis$insert_layer(": "insert_layer(layer, name = NULL, ...)",
        "qgis$layer_info(":   "layer_info(x)",
        "qgis$list_layers(":  "list_layers(type = NULL)"
    }
}